On branch main
Your branch is up to date with 'origin/main'.

Untracked files:
  (use "git add <file>..." to include in what would be added to cache)
        vscode-mcp-extension/node_modules/
        vscode-mcp-extension/package-lock.json

apiVersion: flagger.app/v1beta1
kind: Canary
metadata:
  name: mcp-server
  namespace: zf-mcp
spec:
  provider: nginx
  progressDeadlineSeconds: 600
  
  # Target the primary deployment created by HelmRelease
  targetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mcp-server
  
  # NGINX ingress to manage traffic shifting
  ingressRef:
    apiVersion: networking.k8s.io/v1
    kind: Ingress
    name: mcp-server
  
  service:
    port: 80
    targetPort: 3000
  
  # Analysis defaults (overridden per-env)
  analysis:
    interval: 1m
    threshold: 5
    maxWeight: 50
    stepWeight: 10
    metrics:
      - name: request-success-rate
        thresholdRange:
          min: 99
        interval: 1m
      - name: request-duration
        thresholdRange:
          max: 500
        interval: 1m
=======
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mcp-server
spec:
  interval: 5m
  chart:
    spec:
      chart: mcp
      version: ">=0.1.0"
      sourceRef:
        kind: GitRepository   # or HelmRepository/OCIRepository if you publish charts elsewhere
        name: mcp-chart-repo
        namespace: flux-system
  values:
    image:
      repository: ghcr.io/YOUR_ORG/YOUR_REPO/mcp-server
      # digest is patched per-env from CI (multi-arch manifest digest)
      digest: sha256:TO_BE_PATCHED
    service:
      port: 80
      targetPort: 7337
    ingress:
      enabled: true
      host: to-be-patched.example.com
    autoscaling:
      enabled: false
>>>>>>> 55b453375d4cabd1788fb9ec33f188ecd744c4a1
