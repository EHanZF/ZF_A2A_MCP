apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: mcp-base
  namespace: flux-system
spec:
  interval: 10m
  path: ./environments/base
  prune: true
  sourceRef:
    kind: GitRepository
    name: platform-config   # set to your Flux Source for this repo
