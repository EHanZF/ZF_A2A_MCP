apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: mcp-server
spec:
  values:
    image:
      digest: sha256:TO_BE_PATCHED_BY_CI
    ingress:
      host: dev.mcp.example.com
    autoscaling:
      enabled: true
      minReplicas: 1
      maxReplicas: 3
