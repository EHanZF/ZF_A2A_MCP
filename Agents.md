+## Agents.md
+This repository is structured as a **Vector Store + Tool Surface** for autonomous Agents.
+
+See:
+- `config/vector_spaces.json` for centroids & RASIC embeddings
+- `config/live_plan.json` for deterministic CI/CD routing
+- `adk/a2a_protocol.ts` for PE‑OPS‑PKT‑V1 handshake
+- `adk/security/*` for RBAC, gravity scoring, and gating
