name: zk-verify
on:
  workflow_call: {}
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
        working-directory: zk
      - run: npm test
        working-directory: zk
