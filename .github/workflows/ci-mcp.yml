name: CI – MCP Server

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Avoid overlapping runs on the same ref to reduce flaky results
concurrency:
  group: ci-mcp-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build_and_test:
