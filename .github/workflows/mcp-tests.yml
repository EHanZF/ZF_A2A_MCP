name: MCP Unit Tests (Multi-Python)

on:
  push:
    branches: ["main", "develop", "*"]
  pull_request:
    branches: ["main", "develop", "*"]
  schedule:
    - cron: "0 2 * * *"  # 02:00 UTC nightly
  workflow_dispatch: {}

jobs:
  # ... (same jobs section as above)
