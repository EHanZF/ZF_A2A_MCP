# -------------------------------------------------------------------------
# ZF A2A MCP — Deterministic Tool Bus Validation Workflow
#
# This GitHub Actions workflow validates the canonical MCP Tool Bus used
# for deterministic agent alignment (A2A). It performs:
#
# 1. MCP Manifest Validation
#    - Loads mcp/a2a-mcp.yaml
#    - Ensures strict JSON Schema correctness
#    - Confirms all tools are registered and normalized
#
# 2. Capability Vector Verification
#    - Recomputes tool_vector from the tools list
#    - Ensures it matches:
#         capabilities.tool_vector
#         a2a.canonical_tool_vector
#
# 3. A2A Handshake Simulation
#    - Simulates Agent A and Agent B capability exchange
#    - Confirms both align to the canonical tool bus
#
# 4. Deterministic Output Enforcement
#    - Fails CI if ANY mismatch occurs
#
# This workflow ensures the ZF_A2A_MCP repository always produces
# deterministic, reproducible MCP artifacts for multi-agent systems.
# -------------------------------------------------------------------------
