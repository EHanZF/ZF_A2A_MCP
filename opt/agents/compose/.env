MCP_TAG=latest
MCP_API_KEY=REDACTED
OPENAI_API_KEY=REDACTED
DOMAIN=your.domain
EMAIL=admin@your.domain

cd /opt/agents/compose
docker login ghcr.io -u YOUR_GH_USER -p GHCR_PAT
docker compose pull
docker compose up -d
